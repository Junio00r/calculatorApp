<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Calculadora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!--  Default Style  -->
    <style name="ConfigurationDefaultButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">25sp</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">73dp</item>
        <item name="android:outlineProvider">none</item>
        <item name="android:background">@drawable/button_ripple_effect_animation</item>
    </style>

    <!--  Bottons Types  -->

                <!-- default buttons -->
    <style name="NumberButtonStyle" parent="ConfigurationDefaultButton">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="EspecialButtonStyle" parent="ConfigurationDefaultButton">
        <item name="android:textColor">@color/brown</item>
    </style>


                <!-- Expensive buttons -->
    <style name="ViewPagerButtonsDefault" parent="NumberButtonStyle">
        <item name="android:minWidth">68dp</item>
        <item name="android:minHeight">59dp</item>
    </style>

    <style name="ViewPagerButtonsEspecial" parent="EspecialButtonStyle">
        <item name="android:minWidth">68dp</item>
        <item name="android:minHeight">59dp</item>
    </style>

                <!--  Menu Button  -->
    <style name="MenuButton">
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:maxWidth">200dp</item>
        <item name="android:maxHeight">200dp</item>
        <item name="android:background">@drawable/button_menu</item>
    </style>

    <style name="Theme.Calculadora" parent="Base.Theme.Calculadora" />
</resources>
